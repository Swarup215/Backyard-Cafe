{
  "name": "backyard-cafe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix craving-scraps && npm run build --prefix craving-scraps",
    "start": "npx serve craving-scraps/dist --listen tcp://0.0.0.0:$PORT",
    "dev": "npm run dev --prefix craving-scraps"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
