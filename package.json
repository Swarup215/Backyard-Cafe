{
  "name": "backyard-cafe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:landing": "npm install --prefix craving-scraps",
    "build": "npm install --prefix craving-scraps && npm run build --prefix craving-scraps",
    "start": "npm run preview --prefix craving-scraps",
    "dev": "npm run dev --prefix craving-scraps"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
